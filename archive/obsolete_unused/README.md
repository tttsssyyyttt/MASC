# Obsolete Unused Code

This folder keeps modules that were unused by the active code path or still followed older interfaces.

Archived examples include old trainer classes, the unused QMIX episode buffer, and solver implementations that do not match the current BDQ action format.
