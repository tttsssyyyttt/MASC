# Obsolete Scripts

This folder keeps old experiment, tuning, plotting, validation, and helper scripts.

The active script entry points are:

- `scripts/train.py`
- `scripts/evaluate.py`
- `scripts/full_compare.py`

Everything here is archived because it duplicates those flows, depends on old interfaces, or should be rewritten if needed.
